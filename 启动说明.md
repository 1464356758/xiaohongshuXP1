# XHS-XP1｜启动说明

## 第一次使用
1. 在GPT新项目里建立16个独立聊天。
2. 每个聊天粘贴 `角色/` 下对应的角色指令。
3. 先进入【R01 运营总监AI】。
4. 把下面这句话发给R01：

请接手项目 XHS-XP1，仓库 https://github.com/1464356758/xiaohongshuXP1，分支 main。先真实读取 数据库入口.json、当前进度.json、角色工作流.md 和你的正式角色指令。当前是首次启动，请按ROUTER_BOOTSTRAP开始，不要代替专业角色做内容。完成控制面初始化后，告诉我下一步复制给哪个角色。

5. 以后只跟每个角色最后的“下一步”走。

## 第一次会发生什么
R01不会直接写第一篇。
它会先建立账号长期基础：
R16账号策略 → R07 VOICE → R13 VOICE审核 → R08 PERSONA → R11 PERSONA审核 → R01锁定。

完成后才进入首篇CONTENT。

## 每一篇内容
普通主链：
R02+R03 → R01 → R04 → R01 → R05 → R01 → R06 → R07 → R08 → R09 → R10+R11+R12 → R01 → R13 → R01 → R14 → R13/R01发布许可 → 用户真实发布。

你不需要记这条链，R01会每一步创建正式任务并告诉你该复制给谁。

## 图片
需要实际图片的步骤，直接在对应图片角色聊天里处理。
R09生成后，后续三审必须看当前实际图片或当前manifest绑定的图片版本。

## 发布
只有R01建立CONTENT LOCK并完成发布前Gate后才发布。
真实发布由你本人完成。
发布完成后把真实链接/截图交给R14。

## 重要
- 不使用旧xiaohongshu2作为正式状态。
- 不向xiaohongshuXT写运行内容。
- XP1是这本账号项目唯一运行仓库。
- 角色之间不要靠旧聊天记忆传正式状态，统一读GitHub。
